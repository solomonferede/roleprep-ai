# roleprepai
RolePrepAI is a lightweight web application that generates role-specific interview questions using the Gemini AI API. Built with Django, Gunicorn, and Nginx — deployed on a custom domain with Docker.
